# Optional. Replay mode is the default and needs no external credentials.
# Live mode is server-side only and is pinned to gemini-3.5-flash.
GEMINI_API_KEY=""

# Optional. Defaults to 3000.
PORT=3000
